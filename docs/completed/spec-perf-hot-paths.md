---
title: spec-perf-hot-paths
type: completed-index
---
# spec-perf-hot-paths

- [array-event-payload-guard] REVERTED at HEAD (had landed as 4da08a4). The seven listener guards were removed again by direct edit — the item measured UNPROVEN (see its prior row) and the guard-per-site pattern was judged not worth the code volume. sort()'s listener bail survives, restructured from early-return to if/else. Gate: pnpm agent:test green (tsc --noEmit + 369 tests). · spec: spec-perf-hot-paths
- [array-lazy-listeners] NOT PURSUED. Lazy-null ReactiveArray.listeners until first on(). Dropped for the same reason as heap-packed-array (LOW allocation-shaving class, predicted null); it also depended on array-event-payload-guard and array-sort-listener-bail, both of which landed, so the guards it would have rewritten null-safely remain in their non-null form. · spec: spec-perf-hot-paths
- [heap-packed-array] NOT PURSUED. Filling the heap array with undefined at init and after growth to avoid holey-elements reads. Dropped on the session's measured severity pattern: all three MED-or-below allocation-shaving items (async-probe-typeof-gate, array-event-payload-guard, cleanup-lazy-errors) measured null against ±5-15% bench noise, while all three HIGH/MED-HIGH items landed real wins. This LOW item is the same class. · spec: spec-perf-hot-paths
- [lazy-computed-eval] REJECTED by user. Deferring computed() first evaluation + async probe to first demand was attempted previously (see stash 'lazy-by-default computeds', patch at ~/.claude/storage/lazy-bench/) and reverted per its own ab-gate verdict: it regressed every bench except write. Q1 in the spec index is hereby answered option 3 (reject). Do not re-attempt without a fundamentally different mechanism. · spec: spec-perf-hot-paths
- [cleanup-lazy-errors] REVERTED (no commit). Single-cleanup path calling bare (dropping the errors[] alloc + try/catch frame) and lazy ??= accumulator was implemented, green (tsc + 369 tests + 88 scoped incl errors.test.ts), then reverted as unproven. Evidence: interleaved 4-round A/B on bench/system.bench.ts in the session's QUIETEST window (control spread only -2.0..+5.3%) gave cleanup/dispose targets -0.5% median vs control +1.9%; 'register 1 cleanup' landed at exactly +0.0%. Null is trustworthy here, not noise-masked. · spec: spec-perf-hot-paths
- [array-event-payload-guard] 4da08a4. Seven payload-carrying mutators (set/concat/pop/push/shift/splice/unshift) construct their event object only when a listener for that event exists; clear/reverse untouched (no payload), sort already covered by array-sort-listener-bail — landed by direct-implement. Gate: pnpm agent:test green (tsc --noEmit + 369 tests) + 84 scoped in test/reactive/array.test.ts. Deviations: perf UNPROVEN at bench resolution — interleaved 4-round A/B gave guarded mutators +2.3% median vs control -4.5%, but the listener-present group (where the guard is strictly added work and must be <=0) came in at +7.3%, i.e. noise exceeds the effect; round 2 was disturbed and excluded, which weakened rather than strengthened the result. Kept on user decision as a never-slower allocation removal, not on measured evidence. · spec: spec-perf-hot-paths
- [async-probe-typeof-gate] REVERTED (no commit). typeof gate on the computed() async probe was implemented, green (tsc + 369 tests + 116 scoped incl async-computed/async-iterable), then reverted as unproven. Evidence: interleaved 4-round A/B on bench/system.bench.ts showed creation benches -1.3% median vs control +0.2% median, spread -15.5%..+11.9% — no effect above the noise floor. Isolated microbench (5M iters x 5 rounds) priced the probe at 0.20 ns/op saved for uniform primitive returns (0.08% of ~256 ns creation) rising to 15.17 ns (5.9%) only when one probe site sees many return shapes (IC megamorphism, not primitive boxing as the audit assumed). Do not re-attempt without a workload that provably varies computed return types. · spec: spec-perf-hot-paths
- [array-sort-listener-bail] 69729cd. sort() returns after native super.sort when no 'sort' listener is registered, skipping the before[] copy, Map buckets and order[] payload — landed by direct-implement. Gate: pnpm agent:test green (tsc --noEmit + 369 tests); 4-run baseline vs 4-run after on bench/reactive/array.bench.ts: sort +54.0% median (10 items +54.0%, 100 +57.5%, 1000 +51.7%) vs 23 control benches -2.8% median. Deviations: none — listener-registered arrays take the unchanged path. · spec: spec-perf-hot-paths
- [signal-shape-unification] 947ff39. Signal literal gains state/key/parent so Signal and SelectorSignal share one hidden class and read()/peek() stop taking missing-property loads for state — landed by direct-implement. Gate: pnpm agent:test green (tsc --noEmit + 369 tests); interleaved A/B 4 rounds on system.bench: read signal (no observer) +8.5%, write during stabilization +8.5%, register 1 cleanup +6.0%; median +1.8% across 19 hot benches. Deviations: none — additive fields only, semantics unchanged (0 & STATE_COMPUTED == 0). · spec: spec-perf-hot-paths
- [array-symbol-species] 340b09d. Symbol.species returns Array so splice stops constructing a throwaway ReactiveArray (holey array + signal + listeners) per call — landed by direct-implement. Gate: pnpm agent:test green (tsc --noEmit + 369 tests); interleaved A/B 4 rounds: splice +24.1% median (replace +56.5%, remove +24.1%, insert +17.3%) vs 23 control benches -1.8% median. Deviations: map/filter/slice/flat/flatMap now return plain Array instead of ReactiveArray — accepted by user; public Reactive<T> already typed these U[], so no type-level break. · spec: spec-perf-hot-paths
